# === Backend configuration ===

# AI provider keys
DEEPSEEK_API_KEY=your-deepseek-api-key

# Google Vertex AI credentials
GOOGLE_APPLICATION_CREDENTIALS=backend/keys/scan-ingredients.json
GCP_PROJECT_ID=your-gcp-project
GCP_LOCATION=us-central1

# FastAPI routing / CORS
API_PREFIX=/api
EXPOSE_LEGACY_ROUTES=true
FRONTEND_ORIGINS=http://127.0.0.1:3000

# Database (default to local SQLite)
DATABASE_URL=sqlite:///./data/recipenow.db
AUTO_CREATE_DB=true

# Optional MySQL overrides (leave blank when using SQLite)
# MYSQL_USER=
# MYSQL_PASSWORD=
# MYSQL_HOST=localhost
# MYSQL_PORT=3309
# MYSQL_DB=recipenow
